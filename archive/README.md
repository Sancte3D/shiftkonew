# Archive (orphan patches)

These patches are kept for reference but are not instantiated by `shiftko-main.pd`:

- `shiftko-pedal.pd`
- `shiftko-accent.pd`

Reason: Teil-2 surgical audit marked them as orphaned/disabled in the current runtime architecture.

If reactivation is desired later, instantiate explicitly in `shiftko-main.pd` and add proper transport/gate controls.
